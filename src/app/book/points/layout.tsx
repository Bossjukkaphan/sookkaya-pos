// หน้า page เป็น client component จึงตั้งชื่อแท็บผ่าน layout แทน
export const metadata = { title: "แต้มสะสม" }

export default function PointsLayout({ children }: { children: React.ReactNode }) {
  return children
}
