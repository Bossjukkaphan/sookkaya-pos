# Boss Hub Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** เปลี่ยน `/overview` เป็น Boss Hub — 4 โซนที่ตอบคำถามธุรกิจด้วยประโยคภาษาคนก่อนตัวเลข พร้อมลิงก์เจาะลึกทุกจุด

**Architecture:** pure functions ใหม่ 2 ไฟล์ (`period-compare.ts` กติกาเทียบช่วง, `boss-hub.ts` ประกอบ view-model) + RPC read-only ตัวเดียว `boss_hub_rollup` (รวมยอดรายวัน/รายหมอนวดจาก sales — เลี่ยงเพดาน 1000 แถว) + เขียน `overview/page.tsx` ใหม่เป็น 4 zone components แยกไฟล์ query วิ่งขนานทั้งชุดแบบ `Promise.allSettled` (โซนไหนล้มโซนนั้นแสดงการ์ด error โซนอื่นรอด)

**Tech Stack:** Next.js 16 (ไม่เปิด `cacheComponents`), Supabase (Postgres + RLS), vitest, กราฟ SVG เดิม (`src/components/charts/*`)

**Spec:** `docs/superpowers/specs/2026-08-09-boss-hub-design.md`

## Global Constraints

- **Next.js 16** — ก่อนแตะไฟล์อ่าน guide ใน `node_modules/next/dist/docs/` (ถ้า `node_modules` หายให้ `npm ci`)
- **Migration ผ่าน Supabase MCP `apply_migration` เท่านั้น** (project `jrioyrmicioqammeevgh`) + เก็บสำเนาลง `supabase/migrations/<version>_<name>.sql` — ห้าม CLI push/reset · RPC ใหม่ต้อง `SECURITY INVOKER` + `set search_path = public` + read-only
- **Reuse ห้ามเขียนซ้ำ**: anomaly = `detectAnomalies` ใน `src/lib/expense-analytics.ts` · dormant = query แบบ `crm/customer-insights.tsx` (`v_customer_ltv`) · วันเกิด = ขยาย `src/lib/crm-birthday.ts` (cron ยังใช้ตัวเดิม ห้ามเปลี่ยนพฤติกรรม) · เครดิต = `src/lib/member-credit.ts` · เป้า/run-rate = `targetRunRate`,`isMonthIncomplete` ใน `src/lib/finance.ts` · กราฟ = `GroupedBarChart` เดิม · cache = `getShopSettingsCached`/`getTherapistsCached`
- **กติกาเทียบที่เป็นธรรม (สเปกโซน 1)**: MTD เทียบ MTD ตัดวันเดียวกัน · วันนี้เทียบค่าเฉลี่ยวันในสัปดาห์เดียวกัน 4 สัปดาห์ล่าสุด (ไม่ครบใช้เท่าที่มี + บอกจำนวน) · วันนี้ป้าย "ณ ตอนนี้"
- **ลิสต์ดูแลด่วนเรียง**: วันเกิดวันนี้/พรุ่งนี้ → dormant ≥2 ครั้ง หาย >60 วัน top 5 ตาม LTV → เครดิต bucket ต่ำสุด top 3 · รวมสูงสุด ~10 แถว · แถวกดไป `/crm`
- สิทธิ์เดิม: `canSeeInsights` (manager+) — โครง gate แบบเดียวกับหน้าปัจจุบัน (query ก่อน gate, comment อธิบายไว้แล้ว — คงไว้)
- ทุก task จบด้วย `npm run lint && npm run test` ผ่าน (task ที่แตะหน้า/route เพิ่ม `npm run build`) แล้ว commit ข้อความไทยสไตล์ repo
- comment ไทยอธิบาย "ทำไม" · งานบน branch `claude/system-improvement-brainstorm-102o97` **หลัง PR #1 (Speed Pass) merge แล้วเท่านั้น** — ก่อนเริ่ม Task 1 ต้อง `git fetch origin main && git checkout -B claude/system-improvement-brainstorm-102o97 origin/main` (branch ตั้งต้นใหม่จาก main ที่มี Speed Pass แล้ว)

## File Structure

| ไฟล์ | หน้าที่ |
|---|---|
| `src/lib/period-compare.ts` + `.test.ts` (สร้าง) | กติกาเทียบช่วง: MTD, same-weekday average, ประโยคคำตัดสิน — pure |
| `supabase/migrations/<v>_boss_hub_rollup.sql` (สร้าง) | RPC รวมยอดรายวัน + รายหมอนวด ในช่วงวันที่กำหนด |
| `src/lib/boss-hub.ts` + `.test.ts` (สร้าง) | ประกอบ view-model 4 โซน: care list, top therapists, guarantee flags — pure |
| `src/lib/crm-birthday.ts` (แก้) + `.test.ts` (สร้าง) | เพิ่ม `birthdayUpcomingCustomers()` (วันนี้+พรุ่งนี้) โดยตัวเดิมพฤติกรรมเดิมเป๊ะ |
| `src/app/(app)/overview/verdict-strip.tsx` (สร้าง) | โซน 1 — render จาก view-model |
| `src/app/(app)/overview/money-zone.tsx` (สร้าง) | โซน 2 — กำไร + ชิป anomaly + กราฟ 6 เดือน |
| `src/app/(app)/overview/care-list.tsx` (สร้าง) | โซน 3 — ลิสต์ลูกค้าดูแลด่วน |
| `src/app/(app)/overview/team-zone.tsx` (สร้าง) | โซน 4 — ท็อปหมอนวด + การันตี + สถานะวันนี้ |
| `src/app/(app)/overview/zone-error.tsx` (สร้าง) | การ์ด "โหลดไม่สำเร็จ" ใช้ร่วมทุกโซน |
| `src/app/(app)/overview/page.tsx` (เขียนใหม่) | query ขนาน → lib → 4 โซน |
| `src/app/(app)/overview/loading.tsx` (แก้) | skeleton ตรงโครง 4 โซนใหม่ |
| `src/types/database.ts` (regenerate) | type ของ RPC ใหม่ |

---

### Task 1: `period-compare.ts` — กติกาเทียบช่วง (TDD)

**Files:**
- Create: `src/lib/period-compare.ts`
- Test: `src/lib/period-compare.test.ts`

**Interfaces:**
- Produces (Task 6 ใช้):
  ```ts
  export type Delta = { current: number; previous: number; pct: number | null }
  //  pct = null เมื่อ previous เป็น 0 (ห้ามหารศูนย์ — UI แสดง "เทียบไม่ได้")

  /** ช่วง MTD ของเดือน ym ตัดที่วัน dayOfMonth (รวมวันนั้น) — คืน {from,to} YYYY-MM-DD */
  export function mtdRange(ym: string, dayOfMonth: number): { from: string; to: string }
  /** MTD เดือนก่อนหน้า ตัดวันเดียวกัน — เดือนก่อนสั้นกว่า ตัดที่วันสุดท้ายของเดือนนั้น (31 มี.ค. → 28/29 ก.พ.) */
  export function prevMtdRange(ym: string, dayOfMonth: number): { from: string; to: string }
  /** วันที่ของ "วันในสัปดาห์เดียวกัน" ย้อนหลัง count สัปดาห์ (ไม่รวม todayIso) เรียงใหม่→เก่า */
  export function sameWeekdayDates(todayIso: string, count: number): string[]
  export function delta(current: number, previous: number): Delta
  /** ประโยคคำตัดสินโซน 1 — คืนภาษาไทยพร้อมทิศทาง เช่น "เดือนนี้วิ่งเร็วกว่าเดือนก่อน 12% ณ วันเดียวกัน"
   *  weeksUsed < weeksRequested → ระบุ "(เทียบ n สัปดาห์)" · previous=0 → "ยังเทียบเดือนก่อนไม่ได้ (ไม่มีข้อมูล)" */
  export function verdictSentence(input: {
    mtd: Delta
    today: { revenue: number; weekdayAvg: number | null; weeksUsed: number }
    todayLabel: string   // เช่น "วันเสาร์"
  }): string
  ```

- [ ] **Step 1: เขียน test ก่อน (fail)**

```ts
// src/lib/period-compare.test.ts
import { describe, expect, it } from "vitest"

import {
  delta,
  mtdRange,
  prevMtdRange,
  sameWeekdayDates,
  verdictSentence,
} from "./period-compare"

describe("mtdRange / prevMtdRange", () => {
  it("MTD ปกติ", () => {
    expect(mtdRange("2026-08", 9)).toEqual({ from: "2026-08-01", to: "2026-08-09" })
    expect(prevMtdRange("2026-08", 9)).toEqual({ from: "2026-07-01", to: "2026-07-09" })
  })
  it("ข้ามปี: ม.ค. เทียบ ธ.ค. ปีก่อน", () => {
    expect(prevMtdRange("2026-01", 15)).toEqual({ from: "2025-12-01", to: "2025-12-15" })
  })
  it("เดือนก่อนสั้นกว่า: 31 มี.ค. ตัดที่ 28 ก.พ. (ปีปกติ)", () => {
    expect(prevMtdRange("2026-03", 31)).toEqual({ from: "2026-02-01", to: "2026-02-28" })
  })
  it("ปีอธิกสุรทิน: 30 มี.ค. 2028 ตัดที่ 29 ก.พ.", () => {
    expect(prevMtdRange("2028-03", 30)).toEqual({ from: "2028-02-01", to: "2028-02-29" })
  })
  it("วันแรกของเดือน", () => {
    expect(mtdRange("2026-08", 1)).toEqual({ from: "2026-08-01", to: "2026-08-01" })
  })
})

describe("sameWeekdayDates", () => {
  it("เสาร์ 4 สัปดาห์ล่าสุด ไม่รวมวันนี้ เรียงใหม่→เก่า", () => {
    // 2026-08-08 เป็นวันเสาร์
    expect(sameWeekdayDates("2026-08-08", 4)).toEqual([
      "2026-08-01", "2026-07-25", "2026-07-18", "2026-07-11",
    ])
  })
})

describe("delta", () => {
  it("คำนวณ pct ปกติ", () => {
    expect(delta(112, 100)).toEqual({ current: 112, previous: 100, pct: 12 })
  })
  it("previous = 0 → pct null (ห้ามหารศูนย์)", () => {
    expect(delta(50, 0).pct).toBeNull()
  })
})

describe("verdictSentence", () => {
  it("เดือนวิ่งเร็วกว่า + วันนี้สูงกว่าค่าเฉลี่ย", () => {
    const s = verdictSentence({
      mtd: delta(112000, 100000),
      today: { revenue: 8450, weekdayAvg: 7750, weeksUsed: 4 },
      todayLabel: "วันเสาร์",
    })
    expect(s).toContain("เร็วกว่า")
    expect(s).toContain("12%")
    expect(s).toContain("วันเสาร์")
  })
  it("ไม่มีข้อมูลเดือนก่อน → บอกตรงๆ ไม่โชว์ delta หลอก", () => {
    const s = verdictSentence({
      mtd: delta(50000, 0),
      today: { revenue: 3000, weekdayAvg: null, weeksUsed: 0 },
      todayLabel: "วันอาทิตย์",
    })
    expect(s).toContain("ยังเทียบเดือนก่อนไม่ได้")
    expect(s).not.toContain("%")
  })
  it("ข้อมูลไม่ครบ 4 สัปดาห์ → ระบุจำนวนสัปดาห์ที่ใช้", () => {
    const s = verdictSentence({
      mtd: delta(90000, 100000),
      today: { revenue: 5000, weekdayAvg: 5200, weeksUsed: 2 },
      todayLabel: "วันพุธ",
    })
    expect(s).toContain("2 สัปดาห์")
  })
})
```

- [ ] **Step 2: รันให้ fail** — `npx vitest run src/lib/period-compare.test.ts` → FAIL (module ไม่มี)
- [ ] **Step 3: implement** — pure functions ล้วน คำนวณวันที่แบบ UTC เหมือน lib วันที่ตัวอื่นใน repo (ดู `src/lib/date-range.ts` `addDays` เป็นแบบ) · `prevMtdRange` ใช้ `Math.min(dayOfMonth, daysInMonth(prevYm))` โดย `daysInMonth`/`shiftMonth` import จาก `@/lib/month` — ห้ามเขียนเอง
- [ ] **Step 4: รันให้ผ่าน** — `npx vitest run src/lib/period-compare.test.ts` → PASS แล้ว `npm run lint && npm run test`
- [ ] **Step 5: Commit** — `feat(boss-hub): กติกาเทียบช่วงกลาง — MTD ตัดวันเดียวกัน + ค่าเฉลี่ยวันเดียวกันของสัปดาห์`

---

### Task 2: Migration — RPC `boss_hub_rollup`

**Files:**
- Create: `supabase/migrations/<version>_boss_hub_rollup.sql`
- Regenerate: `src/types/database.ts` (MCP `generate_typescript_types`)

**Interfaces:**
- Produces (Task 6 เรียก): `supabase.rpc("boss_hub_rollup", { p_from, p_to })` → jsonb
  ```json
  { "daily":       [{ "day": "2026-08-01", "revenue": 12345, "bills": 21 }],
    "by_therapist":[{ "therapist_id": "uuid", "revenue": 45600, "sessions": 38 }] }
  ```

- [ ] **Step 1: ดู schema ตาราง `sales` ก่อน** (คอลัมน์วันที่/ยอด/หมอนวด/สถานะ) จาก `src/types/database.ts` และดูว่าหน้า `reports` กรองยอดยังไง (`src/app/(app)/reports/page.tsx` — เอาเงื่อนไขเดียวกัน เช่น คอลัมน์ยอดสุทธิที่ใช้, ตัดรายการยกเลิกไหม) — **กติกายอดต้องตรงกับ reports เป๊ะ** เขียนสิ่งที่พบลง comment ใน SQL
- [ ] **Step 2: เขียน SQL**

```sql
-- รวมยอดจาก sales เป็นรายวัน + รายหมอนวด ในหนึ่ง round trip
-- เหตุที่เป็น RPC: GROUP BY ฝั่ง DB เลี่ยงเพดาน 1000 แถวของ supabase-js
-- (ดึง sales ดิบทั้งเดือนมี ~1500-3000 แถว โดนตัดเงียบ ตัวเลขจะขาด)
-- กติกายอด: <ระบุคอลัมน์/เงื่อนไขที่ตรวจจาก Step 1 — ต้องตรงกับหน้า reports>
create or replace function public.boss_hub_rollup(p_from date, p_to date)
returns jsonb
language sql
stable
security invoker
set search_path = public
as $$
select jsonb_build_object(
  'daily', coalesce((
    select jsonb_agg(jsonb_build_object('day', d.day, 'revenue', d.revenue, 'bills', d.bills) order by d.day)
    from (
      select sale_date as day, sum(final_price) as revenue, count(*) as bills
      from sales
      where sale_date between p_from and p_to
      group by sale_date
    ) d
  ), '[]'::jsonb),
  'by_therapist', coalesce((
    select jsonb_agg(jsonb_build_object('therapist_id', t.therapist_id, 'revenue', t.revenue, 'sessions', t.sessions) order by t.revenue desc)
    from (
      select therapist_id, sum(final_price) as revenue, count(*) as sessions
      from sales
      where sale_date between p_from and p_to and therapist_id is not null
      group by therapist_id
    ) t
  ), '[]'::jsonb)
)
$$;
```
(ชื่อคอลัมน์ `sale_date`/`final_price`/`therapist_id` ให้แก้ตามที่ Step 1 พบจริง — ห้ามเดา)

- [ ] **Step 3: Apply ผ่าน MCP `apply_migration`** ชื่อ `boss_hub_rollup` + เซฟสำเนาไฟล์ (version จาก `supabase_migrations.schema_migrations`)
- [ ] **Step 4: เทียบผลกับ query ตรงบนข้อมูลจริง** ผ่าน `execute_sql` — เดือนปัจจุบันเต็มเดือน: `sum(revenue)` ของ `daily` ต้องเท่า `select sum(...) from sales where ...` ตรงๆ และ `by_therapist` รวมต้อง ≤ ยอดรวม (แถว therapist_id null หายไปตามนิยาม) — ตัวเลขไม่ตรง = หยุด ห้ามไปต่อ
- [ ] **Step 5: Regenerate types** → `src/types/database.ts` (ตรวจ diff additive + header คอมเมนต์ไทยไม่หาย)
- [ ] **Step 6: Gate + commit** — `npm run lint && npm run test` → `feat(boss-hub): RPC boss_hub_rollup รวมยอดรายวัน-รายหมอนวดใน round trip เดียว`

---

### Task 3: ขยาย `crm-birthday.ts` — วันเกิดวันนี้+พรุ่งนี้ (TDD)

**Files:**
- Modify: `src/lib/crm-birthday.ts`
- Test: `src/lib/crm-birthday.test.ts` (สร้างใหม่)

**Interfaces:**
- Produces: `birthdayUpcomingCustomers(supabase, todayIso): Promise<(BirthdayCustomer & { daysUntil: 0 | 1 })[]>` — กติกาเดียวกับ `birthdayTodayCustomers` (มีเบอร์, ไม่ถูกบันทึกผลใน 30 วัน) แต่รวมพรุ่งนี้
- **ห้ามเปลี่ยนพฤติกรรม `birthdayTodayCustomers`** — cron `birthday-reminder` ใช้อยู่

- [ ] **Step 1: test ก่อน (fail)** — mock supabase สไตล์ `overpay-credit-actions.test.ts`:

```ts
// src/lib/crm-birthday.test.ts
import { describe, expect, it, vi } from "vitest"

import { birthdayTodayCustomers, birthdayUpcomingCustomers } from "./crm-birthday"

/** ตาราง customers + crm_contacts ปลอม — คุมข้อมูลเองทุกแถว */
function fakeSupabase(customers: unknown[], contacts: unknown[]) {
  return {
    from: vi.fn((table: string) => {
      const c: Record<string, unknown> = {}
      for (const m of ["select", "not", "eq", "gte"]) c[m] = vi.fn(() => c)
      c.then = (resolve: (v: { data: unknown[] }) => void) =>
        resolve({ data: table === "customers" ? customers : contacts })
      return c
    }),
  } as never
}

const CUST = (id: string, birthday: string) => ({ id, name: id, nickname: null, birthday })

describe("birthdayUpcomingCustomers", () => {
  it("รวมวันนี้ (daysUntil 0) และพรุ่งนี้ (daysUntil 1) — เกินนั้นไม่เอา", async () => {
    const db = fakeSupabase(
      [CUST("today", "1990-08-09"), CUST("tomorrow", "1990-08-10"), CUST("far", "1990-08-12")],
      []
    )
    const r = await birthdayUpcomingCustomers(db, "2026-08-09")
    expect(r.map((x) => [x.id, x.daysUntil])).toEqual([["today", 0], ["tomorrow", 1]])
  })
  it("คนที่ถูกบันทึกผล birthday ใน 30 วันแล้ว ไม่โผล่", async () => {
    const db = fakeSupabase([CUST("a", "1990-08-09")], [{ customer_id: "a" }])
    expect(await birthdayUpcomingCustomers(db, "2026-08-09")).toEqual([])
  })
  it("ข้ามเดือน: วันนี้สิ้นเดือน พรุ่งนี้คือวันที่ 1", async () => {
    const db = fakeSupabase([CUST("next", "1990-09-01")], [])
    const r = await birthdayUpcomingCustomers(db, "2026-08-31")
    expect(r.map((x) => [x.id, x.daysUntil])).toEqual([["next", 1]])
  })
  it("birthdayTodayCustomers เดิมพฤติกรรมเดิม (วันนี้เท่านั้น)", async () => {
    const db = fakeSupabase([CUST("today", "1990-08-09"), CUST("tmr", "1990-08-10")], [])
    const r = await birthdayTodayCustomers(db, "2026-08-09")
    expect(r.map((x) => x.id)).toEqual(["today"])
  })
})
```

- [ ] **Step 2: รันให้ fail** — `npx vitest run src/lib/crm-birthday.test.ts`
- [ ] **Step 3: implement** — refactor ภายใน: ฟังก์ชันเดิมกับตัวใหม่ใช้ query ก้อนเดียวกัน (ดึง customers+contacts เหมือนเดิม) ต่างแค่ filter `daysUntilBirthday(...) === 0` vs `<= 1` · ตัวใหม่แนบ `daysUntil` · เรียงวันนี้ก่อนพรุ่งนี้
- [ ] **Step 4: รันให้ผ่าน** + `npm run lint && npm run test` (test เดิมทั้ง repo ต้องผ่าน — โดยเฉพาะของ cron/daily-report ที่แตะไฟล์นี้ทางอ้อม)
- [ ] **Step 5: Commit** — `feat(boss-hub): birthdayUpcomingCustomers วันนี้+พรุ่งนี้ — กติกาเดิม cron ไม่กระทบ`

---

### Task 4: `boss-hub.ts` — view-model 4 โซน (TDD)

**Files:**
- Create: `src/lib/boss-hub.ts`
- Test: `src/lib/boss-hub.test.ts`

**Interfaces:**
- Consumes: `Delta` จาก Task 1 · `BirthdayCustomer & {daysUntil}` จาก Task 3 · `creditBucket`/`summarizeCredit` จาก `@/lib/member-credit`
- Produces (Task 5-6 ใช้):
  ```ts
  export type CareReason = "birthday" | "dormant" | "low_credit"
  export type CareItem = {
    customerId: string
    name: string
    reason: CareReason
    badge: string        // "🎂 วันเกิดวันนี้" | "🎂 พรุ่งนี้" | "💤 หาย 74 วัน" | "💳 เหลือ 120฿"
    amountLabel: string  // LTV หรือเครดิตคงเหลือ format แล้ว
  }
  /** รวม 3 แหล่ง เรียง: วันเกิด → dormant(top5 ตาม ltv) → เครดิตต่ำ(top3) รวมไม่เกิน 10 */
  export function buildCareList(input: {
    birthdays: { id: string; name: string; nickname: string | null; daysUntil: 0 | 1 }[]
    dormant: { id: string; name: string; ltv: number; daysSinceVisit: number }[]
    lowCredit: { id: string; name: string; balance: number }[]
  }): CareItem[]

  export type TherapistRank = {
    therapistId: string; name: string; revenue: number; sessions: number
    sharePct: number     // สัดส่วนเทียบ top1 (ใช้วาด bar ในแถว)
  }
  export function topTherapists(
    byTherapist: { therapist_id: string; revenue: number; sessions: number }[],
    names: Map<string, string>,
    limit: number
  ): TherapistRank[]

  /** หมอที่ "กินการันตี" เกินครึ่งของวันทำงานเดือนนี้ — คืนรายชื่อพร้อมสัดส่วน */
  export function guaranteeFlags(
    days: { therapist_id: string; hitGuarantee: boolean }[],
    names: Map<string, string>
  ): { name: string; ratio: number }[]
  ```

- [ ] **Step 1: test ก่อน (fail)**

```ts
// src/lib/boss-hub.test.ts
import { describe, expect, it } from "vitest"

import { buildCareList, guaranteeFlags, topTherapists } from "./boss-hub"

describe("buildCareList", () => {
  it("เรียง วันเกิด → dormant → เครดิต และตัดที่ 10 แถว", () => {
    const r = buildCareList({
      birthdays: [{ id: "b1", name: "บี", nickname: null, daysUntil: 0 }],
      dormant: Array.from({ length: 7 }, (_, i) => ({
        id: `d${i}`, name: `ด${i}`, ltv: 1000 - i, daysSinceVisit: 70 + i,
      })),
      lowCredit: Array.from({ length: 4 }, (_, i) => ({
        id: `c${i}`, name: `ซ${i}`, balance: 100 + i,
      })),
    })
    expect(r[0].reason).toBe("birthday")
    expect(r.filter((x) => x.reason === "dormant")).toHaveLength(5) // top 5
    expect(r.filter((x) => x.reason === "low_credit")).toHaveLength(3) // top 3
    expect(r.length).toBeLessThanOrEqual(10)
  })
  it("dormant เรียงตาม ltv มาก→น้อย", () => {
    const r = buildCareList({
      birthdays: [],
      dormant: [
        { id: "low", name: "น้อย", ltv: 100, daysSinceVisit: 90 },
        { id: "high", name: "มาก", ltv: 9999, daysSinceVisit: 61 },
      ],
      lowCredit: [],
    })
    expect(r[0].customerId).toBe("high")
  })
  it("ทุกแหล่งว่าง → ลิสต์ว่าง (UI แสดง 'ไม่มีเคสด่วน')", () => {
    expect(buildCareList({ birthdays: [], dormant: [], lowCredit: [] })).toEqual([])
  })
})

describe("topTherapists", () => {
  it("เรียงตามรายได้ แนบชื่อ และ sharePct เทียบ top1", () => {
    const r = topTherapists(
      [
        { therapist_id: "a", revenue: 500, sessions: 5 },
        { therapist_id: "b", revenue: 1000, sessions: 8 },
      ],
      new Map([["a", "ครูเอ"], ["b", "ครูบี"]]),
      5
    )
    expect(r[0]).toMatchObject({ name: "ครูบี", sharePct: 100 })
    expect(r[1]).toMatchObject({ name: "ครูเอ", sharePct: 50 })
  })
  it("ไม่รู้จักชื่อ (id ไม่อยู่ใน map) → ข้ามแถวนั้น", () => {
    const r = topTherapists([{ therapist_id: "x", revenue: 100, sessions: 1 }], new Map(), 5)
    expect(r).toEqual([])
  })
})

describe("guaranteeFlags", () => {
  it("เกินครึ่งของวันทำงาน → ติดธง · ครึ่งพอดีไม่ติด", () => {
    const days = [
      ...Array.from({ length: 3 }, () => ({ therapist_id: "a", hitGuarantee: true })),
      ...Array.from({ length: 2 }, () => ({ therapist_id: "a", hitGuarantee: false })),
      ...Array.from({ length: 2 }, () => ({ therapist_id: "b", hitGuarantee: true })),
      ...Array.from({ length: 2 }, () => ({ therapist_id: "b", hitGuarantee: false })),
    ]
    const names = new Map([["a", "ครูเอ"], ["b", "ครูบี"]])
    const r = guaranteeFlags(days, names)
    expect(r).toHaveLength(1)          // a = 3/5 ติด · b = 2/4 ครึ่งพอดี ไม่ติด
    expect(r[0]).toMatchObject({ name: "ครูเอ" })
  })
})
```

- [ ] **Step 2: รันให้ fail** → **Step 3: implement** (pure ล้วน ใช้ `formatBaht` จาก `@/lib/constants` ประกอบ label) → **Step 4: รันผ่าน + gate เต็ม** → **Step 5: Commit** — `feat(boss-hub): view-model 4 โซน — care list, ท็อปหมอนวด, ธงการันตี`

**หมายเหตุนิยาม "กินการันตี":** ก่อน implement ให้เปิด `src/app/(app)/commission/page.tsx` (~บรรทัด 72-90) ดูว่า `v_therapist_daily` บอก "วันนั้นการันตีถูกเติม" ผ่านคอลัมน์ไหน (`guarantee_amount > 0` หรือเทียบ `net_commission` กับ `total_commission`) — page.tsx (Task 6) ต้องแปลงเป็น `hitGuarantee: boolean` ตามนิยามจริงนั้น แล้วบันทึกนิยามที่ใช้ลง comment ของ `guaranteeFlags`

---

### Task 5: Zone components (render ล้วน)

**Files:**
- Create: `src/app/(app)/overview/verdict-strip.tsx`, `money-zone.tsx`, `care-list.tsx`, `team-zone.tsx`, `zone-error.tsx`

**Interfaces:**
- Consumes: types จาก Task 1/4 · `GroupedBarChart` (`Series` จาก `@/components/charts/grouped-bar-chart`) · `Card*` จาก `@/components/ui/card` · `formatBaht`
- Produces (Task 6 ใช้): components รับ **ข้อมูลสำเร็จรูปเท่านั้น ไม่ยิง query เอง**:
  - `VerdictStrip({ sentence, mtd, todayRevenue, todayBills, target })` — target: `{ amount: number; achieved: number; runRate: number } | null`
  - `MoneyZone({ headline, profit, margin, profitDelta, anomalyChips, chart })` — anomalyChips: `{ label: string; level: "alert"|"warn" }[]` (สูงสุด 3, ว่าง → แสดง "รายจ่ายปกติดี ✓") · chart: `Series[]` 6 เดือน
  - `CareList({ items }: { items: CareItem[] })` — ว่าง → "ไม่มีเคสด่วนวันนี้ ✓" · แถวเป็น `<Link href={`/crm?focus=${customerId}`}>` (ตรวจก่อนว่า `/crm` รองรับ query param ไหน — ถ้าไม่มีให้ลิงก์ `/crm` เฉยๆ และบันทึกใน report)
  - `TeamZone({ headline, top, flags, todayStatus })` — top: `TherapistRank[]` · flags: ผลจาก `guaranteeFlags` · todayStatus: `{ checkedIn: number; queueTotal: number; queueDone: number }`
  - `ZoneError({ zone }: { zone: string })` — การ์ด "โหลดไม่สำเร็จ ลองรีเฟรช"
- ทุกโซนมีลิงก์เจาะลึกตามสเปก (โซน 2 → `/finance`,`/insights/expenses` · โซน 3 → `/crm` · โซน 4 → `/commission/summary`,`/team`,`/checkin`) · โทนสี/สไตล์ตามหน้าเดิม (`#664343`, `Card` เดิม)

- [ ] **Step 1: เขียนทั้ง 5 ไฟล์** — ดูโครง JSX/คลาสจาก `overview/page.tsx` เดิม (hero gradient, StatCard) เป็นแบบ อย่าประดิษฐ์ภาษาภาพใหม่
- [ ] **Step 2: Gate** — `npm run lint && npm run test && npm run build` (ยังไม่มีใคร import — build ต้องผ่าน)
- [ ] **Step 3: Commit** — `feat(boss-hub): zone components 4 โซน + การ์ด error รายโซน`

---

### Task 6: เขียน `overview/page.tsx` ใหม่ — ประกอบทุกอย่าง

**Files:**
- Modify (เขียนใหม่): `src/app/(app)/overview/page.tsx`
- Modify: `src/app/(app)/overview/loading.tsx`

**Interfaces:**
- Consumes: ทุกอย่างจาก Task 1-5 + ของ reuse ตาม Global Constraints

- [ ] **Step 1: วาง data flow** — query ทั้งหมดใน `Promise.allSettled` ก้อนเดียว:
  1. `getMyProfile()` (gate)
  2. `boss_hub_rollup(mtdRange)` + `boss_hub_rollup(prevMtdRange)` + `boss_hub_rollup` ของวัน `sameWeekdayDates(today,4)` (ยิงเป็นช่วง `[เก่าสุด..วันนี้]` ครั้งเดียวแล้วเลือกวันจาก `daily` ฝั่ง JS — ไม่ยิง 6 ครั้ง)
  3. `v_monthly_pl` 6 เดือน (กราฟ + กำไร MTD ใช้ rollup ไม่ใช่ view เพราะ view เป็นรายเดือนเต็ม — กำไร MTD = รายรับ MTD จาก rollup − รายจ่าย MTD จาก expenses query ช่วงเดียวกัน ตามสูตร accrual ของ `/finance` ดู `src/lib/finance.ts`)
  4. `expenses` เดือนปัจจุบัน + 3 เดือนย้อน (input ของ `detectAnomalies` — เรียกแบบเดียวกับ `src/app/(app)/insights/expenses/page.tsx` เปิดดู call จริงก่อน)
  5. `birthdayUpcomingCustomers(supabase, today)`
  6. dormant: query `v_customer_ltv` แบบเดียวกับ `crm/customer-insights.tsx` (~บรรทัด 34-50) — ≥2 ครั้ง, last_visit เกิน 60 วัน, เรียง ltv, limit 5
  7. เครดิต: ก้อน query member_topups/balances เดิมของหน้า (ยกมาทั้ง comment เรื่องเพดาน 1000 แถว) → `creditBucket` เอา bucket ต่ำสุด top 3
  8. `getTherapistsCached()` (ชื่อหมอ) + `v_therapist_daily` เดือนนี้ (ธงการันตี) + `attendance` วันนี้ (นับเช็คอิน) + `queue_entries` วันนี้ (total/done)
  9. `getShopSettingsCached()` (เป้าเดือน) — ใช้ `targetRunRate` เดิม
- [ ] **Step 2: zone-level error containment** — ผล `allSettled` แบ่งเป็นกลุ่มต่อโซน: โซนไหนมี query rejected → render `<ZoneError zone="..."/>` โซนอื่นปกติ · gate `canSeeInsights` ยังอยู่ตำแหน่งเดิม (หลัง batch, ก่อน render) พร้อม comment เดิม
- [ ] **Step 3: layout ตามสเปก** — มือถือ: โซน 1→2→3→4 เรียงลง การ์ดเต็มกว้าง · จอกว้าง (≥lg): โซน 1 เต็มแถวบน, ล่าง `lg:grid-cols-3`: โซน 2 กิน `lg:col-span-2`, โซน 3+4 ซ้อนกันในคอลัมน์ขวา
- [ ] **Step 4: ปรับ `loading.tsx`** — โครง 4 โซนใหม่ (แถบ verdict เต็มกว้าง + กริดล่างตาม Step 3) ใช้ `SkeletonBar`/`SkeletonCard` เดิม
- [ ] **Step 5: Gate เต็ม** — `npm run lint && npm run test && npm run build`
- [ ] **Step 6: Commit** — `feat(boss-hub): หน้าภาพรวมโฉมใหม่ — 4 โซนตอบคำถามธุรกิจ query ขนานทั้งชุด`

---

### Task 7: ปิดงาน — ตรวจของเดิมมีที่ไป + สถานะสเปก

**Files:**
- Modify: `docs/superpowers/specs/2026-08-09-boss-hub-design.md` (บรรทัดสถานะ)

- [ ] **Step 1: ไล่ตาราง "ของเดิมใน overview ไปไหน" ในสเปก** — เช็คทีละแถวกับหน้าจริงว่าตรง (เป้า/run-rate อยู่โซน 1, กราฟอยู่โซน 2, เครดิต top3 อยู่โซน 3, YTD/ตาราง 6 เดือนถูกตัดจริง) — แถวไหนไม่ตรงกลับไปแก้ Task 6 ก่อน
- [ ] **Step 2: Gate สุดท้าย** — `npm run lint && npm run test && npm run build`
- [ ] **Step 3: อัปเดตสถานะสเปก** → `implement เสร็จแล้ว — รอเจ้าของร้านลองใช้จริง` + commit `docs(boss-hub): ปิดสถานะสเปก` + push branch

---

## หมายเหตุลำดับงาน

- **ห้ามเริ่ม Task 1 จนกว่า PR #1 (Speed Pass) จะ merge** แล้ว reset branch จาก `origin/main` ตามขั้นตอนใน Global Constraints — มิฉะนั้นโค้ด Boss Hub จะไหลเข้า PR เดิม
- Task 1, 3, 4 เป็น pure lib ไม่พึ่ง migration — ทำก่อน Task 2 ได้ถ้า MCP ไม่พร้อม แต่ Task 6 ต้องรอครบทุกตัว
